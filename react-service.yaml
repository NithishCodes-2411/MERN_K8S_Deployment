iapiVersion: v1
kind: Service
metadata:
  name: node-service
  labels:
    app: node-app
spec:
  selector:
    app: node-app 
  ports:
    - protocol: TCP
      port: 1026
      targetPort: 1026
  clusterIP: None     

