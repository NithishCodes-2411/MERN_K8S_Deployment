apiVersion: v1
kind: Pod
metadata:
  name: node-pod
spec:
  containers:
  - name: node-container
    image: nithishdocker2411/serversplitapp:latest  # Replace with your Docker image
    ports:
    - containerPort: 80
  imagePullSecrets:
  - name: regcred 
